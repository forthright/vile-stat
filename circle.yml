machine:
  node:
    version: 4.4.0

dependencies:
  pre:
    - sh bin/check-libstdc-deb
  post:
    - npm run compile
    - npm run compile-dev

test:
  override:
    - echo "TODO"
  post:
    - npm run vile
